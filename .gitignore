.DS_Store
**/.DS_Store
**/._.DS_Store

screenshots/
venv/

# Compiled MkDocs output. `mkdocs build` writes it here by default; it is a
# build artefact of docs/ and must never be committed. Build to a scratch
# directory instead: `mkdocs build --strict -d /tmp/bambuddy-wiki-site`.
site/
.cache/
